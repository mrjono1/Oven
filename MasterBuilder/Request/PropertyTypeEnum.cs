namespace MasterBuilder.Request
{
    public enum PropertyTypeEnum
    {
        Uniqueidentifier,
        String,
        Integer,
        DateTime,
        Boolean,
        Relationship
    }
}